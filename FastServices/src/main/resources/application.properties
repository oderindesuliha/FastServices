spring.application.name=FastServices
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/fastservice}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

app.jwt-secret=${APP_JWT_SECRET:6543210b8745d9712675f898ebdd643ac0aa878313567452c4e7c6f3a9d1e5f2}
app.jwt-expiration-milliseconds=${APP_JWT_EXPIRATION_MILLISECONDS:604800000}

spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME}
spring.mail.password=${SMTP_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.config.import=optional:file:.env[.properties]


server.port=${SERVER_PORT:8080}

cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}

spring.h2.console.enabled=false
spring.sql.init.mode=never


# Database Configuration
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/fastservice
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=Password

# JWT Configuration
APP_JWT_SECRET=6543210b8745d9712675f898ebdd643ac0aa878313567452c4e7c6f3a9d1e5f2
APP_JWT_EXPIRATION_MILLISECONDS=604800000

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=oderindesuliha@gmail.com
SMTP_PASSWORD=tprdbnzeriqdizmm

# Server Configuration
SERVER_PORT=8080
CORS_ALLOWED_ORIGINS=*